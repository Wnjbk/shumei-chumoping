# ILI79505A HNH07 维护交接

当前屏的安装与核查以 [README.md](README.md) 和已验收的 `install.sh` 为准；本分支不使用 BOE/GT911 的面板初始化或卸载 I²C 再重新绑定的流程。原 BOE/GT911 适配记录留在仓库 `master` 历史中。

已知可用组合：树莓派 4B、64 位系统、内核 `6.18.34+rpt-rpi-v8`；2-lane DSI 视频 720×1280，触摸与屏幕在 I²C-10 的 `0x41` 共用 Ilitek V3 驱动。驱动执行 TEST 182 步和 2-lane 验证后启用背光；触摸通过直接 I²C 轮询，不以 TCA9555 P03 状态作为读取门槛。

运行中固件 AP 128.0.5.0、MP 128.0.1.0；`.ili` 文件作为驱动编译依赖一同存放，不代表安装会刷固件。桌面横屏 `kanshi transform 270`，对应实测触摸 `base=normal rotate=0`，labwc 单位矩阵。已完成清理旧安装项、部署脚本重装及重启验证，用户现场确认显示与触摸均正常。旧配置和模块先备份再替换，备份在板上源码目录的 `backups/` 中，未上传至 Git。
