# 变更记录

## 计划：新增新屏适配交接文档

- 日期：2026-09-22
- 范围：新增 `NEW_PANEL_HANDOFF.md`，汇总当前 BOE BV050FWM/GT911 驱动基线、新板 64 位验证状态、已知风险与新屏适配步骤。
- 预期：新会话可在不依赖聊天上下文的情况下继续新屏适配。

## 完成：新增新屏适配交接文档

- 日期：2026-09-22
- 文档提交：`cd54359b572f12ad5de35873179e44f98bb60fb8`
- 变更文件：`NEW_PANEL_HANDOFF.md`、`变更记录.md`
- 验证：检查仓库提交历史、当前脚本/驱动源码和已记录的 Raspberry Pi 4B 6.18.34 面板模块编译结果；文档未声称未经实机验证的新屏可用。
- 回退：`git revert cd54359b572f12ad5de35873179e44f98bb60fb8`

## 计划：以用户提供的 ZIP 为基线重建 ILI79505A 面板驱动

- 日期：2026-09-23。ZIP SHA-256 `03CF03A413BAB1DB49B7FF6C541D99286B58D4D38533C786033E058B45BFF896`；从 ZIP 单独解压保留旧项目，旧 ILI9881 文件更名为 legacy 参考文件，新显示模块命名 `panel-ili79505a.ko`。
- 背光前初始化依据供应商 TEST 文件的全部 180 条 REGISTER、两次 DELAY，带 27 字节参数的 Gamma E0/E1 必须原样写入；复用现有 Ilitek V3 I2C 模块的 Linux `i2c_transfer` 路径，不发送额外 DSI 初始化数据。现有触摸驱动 probe 会复位芯片，不采用点屏后卸载 I2C 再绑定触摸导致复位的方案。
- 先确认控制器多字节 DDI I2C 写入协议，再在树莓派持久目录编译，保留旧驱动、设备树及桌面配置可回退；用户确认实屏之前不提交 Git。

## 实机验证：ILI79505A 已点屏

- 日期：2026-09-23。新项目保留在树莓派 `/home/wnk/ili79505a-zip-base/`，Ilitek V3 和 `panel-ili79505a.ko` 均针对 `6.18.34+rpt-rpi-v8` 编译成功；编译有旧源码未使用函数及缺少 MODULE_DESCRIPTION 的非致命警告。
- 本次源码补充 `ilitek_v3/ilitek_v3_ic.c` 中的两处背光前回读门槛：第 6 页 D9=0F、第 0E 页 00=A0。背光前运行 TEST 的 182 步（180 条 REGISTER、2 次 DELAY）；DSI 仅承载 2-lane RGB888 视频。
- 启动后 `lsmod` 显示 `panel_ili79505a` 与 `ilitek_v3_driver`；内核日志记录 2-lane/1280 行验证成功及 TEST 182 步传输；DRM 枚举模式为 720x1280。用户现场确认屏幕点亮。尚未验证多次重启稳定性、完整 Gamma 回读或后续带触摸模组。
- 原配置、旧 DTBO 和旧模块备份于树莓派 `/home/wnk/ili79505a-zip-base/backup-20260923/`；当前未提交 Git，等待用户确认画面与重启稳定性。

## 计划：验证带触摸模组的 P03 轮询

- 日期：2026-09-23。当前输入设备虽注册为 `ILITEK_TDDI`，但 P03 轮询未开启；增加 `ilitek-v3-polling.conf` 的 `polling=1` 模块选项，重启后核查触摸事件与现有屏幕显示。
- 用户确认目前不需要读取 Flash；已取消拟议的导出路径改动，不触发固件读取、刷写或复位，仅验证 P03 轮询是否能让触摸事件上报。
- 保留改动前驱动、开机配置的备份；显示和触摸实测通过之前不提交 Git。

## 结果：轮询已生效，触摸事件待确认

- 日期：2026-09-23。只安装 `/etc/modprobe.d/ilitek-v3-polling.conf`，未更新内核模块、显示设备树及屏幕固件；重启后 `/sys/module/ilitek_v3_driver/parameters/polling` 为 `Y`，日志确认 P03 每 10 毫秒轮询，显示 TEST 182 步照常执行。
- `libinput debug-events --device /dev/input/event4` 两次采样均识别 `ILITEK_TDDI`，但采样期间没有记录到触点事件；尚不能确定采样期间用户是否实际触摸。P03 当前空闲读取为低电平，后续需在按压期间同步观察 P03 和输入事件。
- 用户不需要从片上 Flash 导出固件，未执行固件读取、擦写；导出路径源码改动已撤回。回退轮询：移走新增的 `/etc/modprobe.d/ilitek-v3-polling.conf` 后重启。未提交 Git。

## 计划：改善 P03 轮询偶发漏触

- 日期：2026-09-23。用户确认新模组偶尔能触摸；实机内核为 `CONFIG_HZ=250`，此前延迟工作 `msecs_to_jiffies(10)` 按内核时钟节拍约 12 毫秒轮询。
- 仅调整 `ilitek_v3/ilitek_v3_rpi.c` 的 P03 轮询到每个内核节拍（约 4 毫秒），增加 `ilits->boot` 启动检查并在 I2C/GPIO 操作前避开被显示初始化持有的 `touch_mutex`；备份当前可用模块后在树莓派持久目录编译、更换模块并实屏验证，不动固件和显示驱动。测试稳定前不提交。

## 结果：4 毫秒轮询已部署，现场稳定性待确认

- 修改文件：`ilitek_v3/ilitek_v3_rpi.c`；树莓派持久目录编译成功，安装后重启日志确认 `P03 polling enabled at 4 ms`、显示 TEST 182 步成功，固件版本 AP 128.0.5.0、MP 128.0.1.0 未变。旧模块保存在 `/home/wnk/ili79505a-zip-base/backup-20260923/ilitek_v3_driver.ko.before-poll4ms-20260923`。
- 远程 35 秒 `libinput` 观察期只见输入设备注册、未捕获按压事件；尚不清楚当时用户是否持续触摸。仍需用户现场比较漏触频率，再决定是否采用更可靠的中断/状态读取方案。未提交 Git。

## 计划：对齐旧 GT911 的直接 I2C 轮询方式

- 日期：2026-09-23。用户确认采样期间持续按压，当前 P03 门控造成漏触。旧 `gt911_poll.c` 每轮直接从芯片读取状态、不依赖 GPIO 电平；ILI79505A 没有已知相同状态寄存器，改为每 10 毫秒通过同一 I2C client 读取 Ilitek 触摸报告。
- 在 `ilitek_v3_i2c.c` 新增不打印空闲读错的轮询读取接口，在 `ilitek_v3.c` 的既有触摸报告解析函数增加轮询模式：读错/校验失败/未知空闲包直接忽略，禁止把轮询空闲报文送入复位恢复；在 `ilitek_v3_rpi.c` 删除 P03 电平门槛。正常中断调用保持原有语义。备份当前模块后树莓派持久目录编译、实机验证，固件与显示模块不变，未经验证不提交。

## 实机结果：直接轮询触摸正常

- 日期：2026-09-23。修改 `ilitek_v3/ilitek_v3.c`、`ilitek_v3/ilitek_v3_i2c.c`、`ilitek_v3/ilitek_v3_rpi.c`、`ilitek_v3/ilitek_v3.h`，沿用 `/etc/modprobe.d/ilitek-v3-polling.conf`。树莓派持久目录内为内核 `6.18.34+rpt-rpi-v8` 编译成功；仍有供应商源码原先未使用 ISR 函数及缺少 MODULE_DESCRIPTION 的非致命警告。
- 用户现场确认新模组触摸现已正常；开机日志保留 TEST 182 步、`polling=Y`，DRM 720x1280，固件 AP 128.0.5.0、MP 128.0.1.0。日志未见报告校验错误或 I2C wrapper 错误；未刷写或导出固件。
- 前一版模块备份在 `/home/wnk/ili79505a-zip-base/backup-20260923/ilitek_v3_driver.ko.before-direct-poll-20260923`，需要回退时复制回内核模块路径并重新运行 depmod、重启。用户此前要求确定能用才提交；本次现场正常，继续保留源码工作树未提交，待稳定性观察后决定提交。

## 触摸校准脚本同步（2026-09-23）

- 计划：把已经部署在树莓派 `/home/wnk/shumei-chumoping/touch_calib.py` 的 Ilitek 安全适配同步到本 ZIP 基线的同名 `touch_calib.py`，避免日后重新安装时误用旧的 GT911 解绑定版本；只同步脚本及本变更记录，不修改固件或内核模块。
- 验证：原脚本 `touch_calib show` 识别 `ILITEK_TDDI`，`set rotate 180` 在保留原先实际矩阵 `-1 0 1 0 1 0` 的情况下成功向运行中 labwc 重新加载配置；当前用户尚未选择和验收最终触摸方向，不提交。
- 同步结果：两处项目 `touch_calib.py` 与树莓派已安装脚本 SHA-256 均为 `9AA04359484FC88FF9131E7B3E1D963F86B5DA8F232FEC1BE18CEAFCBB3F62B3`；用户正在自行调节，目前只读观察到 `base=normal rotate=0`，labwc 生效矩阵为单位矩阵。无固件/驱动改动，不提交。

## 计划：改造 ILI79505A 自动部署脚本（2026-09-23）

- 用户已确认横屏显示、触摸直接轮询和 `base=normal rotate=0` 正常；改写 `install.sh`，从持久源码目录依次构建 Ilitek 和面板、安装新的设备树、启动配置及校准工具，不再安装 BOE/GT911、从 `/tmp` 编译或执行校准重置。
- 安装前备份将被覆盖的系统文件及用户配置；原有 labwc 校准、kanshi 配置和保存的触摸状态原则上保持不变。对新系统补足横屏及单位矩阵，固件不自动刷写；测试脚本语法和检查模式，在用户验收前不提交 Git。

## 结果：ILI79505A 自动部署脚本已改造（2026-09-23）

- 修改 `install.sh`：增加只读 `--check`、按实际登录用户选择 home、必要时安装编译依赖；在项目持久目录先编译 `ilitek_v3_driver.ko`，再用其 `Module.symvers` 编译 `panel-ili79505a.ko`，生成并安装设备树；启用 Ilitek 直接轮询，归档旧 GT911/BOE 自动加载配置。本次替换目标统一备份到项目 `backups/install-时间戳-PID/`。
- 安装校准工具时仅在新系统缺失配置时设置 `base=normal rotate=0`、DSI-1 横屏 `transform 270` 和 labwc 单位矩阵；已有用户校准不重置。脚本不刷写固件、不主动加载/卸载模块、不自动重启，提示安装后由用户选择重启。
- 已上传脚本到树莓派持久目录 `/home/wnk/ili79505a-zip-base/install.sh`；本地/远程 SHA-256 一致 `770696d6ac670bb78b1a8b39ed96cc3fdd22877d4985d3ff5201e8189afb377b`。实机上 `bash -n`、`--check --user wnk` 和嵌入 Python 语法检查通过；读取确认当前 `base=normal rotate=0`、显示 overlay 不变。**尚未执行安装、未重启，实际重装流程尚待验收**，未提交 Git。未创建一次性辅助脚本。

## 计划：发布已验收的 ILI79505A 分支（2026-09-23）

- 用户确认清除旧安装项、重新执行 `install.sh`、重启后显示与触摸仍正常。以 GitHub 原仓库 `origin/master` 为父提交，在当前 ZIP 基线项目单独建立 `ili79505a-hnh07` 分支，不使用旧项目有 26 个未上传提交及实验改动的工作区。
- 整理新屏面板、设备树、TEST 初始化表、Ilitek 直接轮询触摸、构建所需固件数据、部署及校准脚本；更新首页与样例配置，标明新屏接线/编译/安装/验证/回退，并排除构建产物与实机备份。仅提交与本分支相关文件，不带本地账号凭据和编译生成物；确认 GitHub 推送结果后记录提交与验证。

## 实机验收与分支提交准备（2026-09-23）

- 在树莓派持久目录先将原安装的两个模块、overlay、旧自动加载与轮询配置、旧项目 `install.sh` 和原校准入口移到 `/home/wnk/ili79505a-zip-base/backups/redeploy-20260923-191655/removed/`，再运行本项目 `install.sh --user wnk`；构建、安装、重启成功。脚本自动备份位于 `/home/wnk/ili79505a-zip-base/backups/install-20260923-191659-6111/`。
- 重启后两个模块均加载，`polling=Y`，内核日志确认 10 ms 轮询、2-lane 面板寄存器核验及 I²C TEST 初始化 182 步；DRM `DSI-1` 已连接，模式 720×1280；`touch_calib show` 识别 `ILITEK_TDDI`，保存与生效矩阵均为 `base=normal rotate=0` 单位矩阵。用户现场再次确认显示和触摸效果依然很好。
- 与树莓派已验收持久源码比较，安装脚本及 17 个实际源文件/固件 SHA-256 一致（树莓派产生的 `ilitek_v3_driver.mod.c` 为编译产物，不纳入）；供应商 Ilitek C 源码原有空白字符问题保留原样，不在此次发布前做无关机械改写。分支差异仅限本屏交付，旧屏专用文件只从新分支追踪范围移除，原仓库 `master` 不变。
